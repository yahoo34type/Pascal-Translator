program example ( input, output );
const c = -3;
b = 0.00002;
var a : 'a' .. 'c';
k, i : integer;
f : real;
begin
read ( k, i ); // program program program
for a := 'a' to 'c' do
case a of
'k': i := i * k;
'f' : a := -882.1444e11;
'2' : i := i + 1;
'i' : k := k + 2;
'b': i := i - k;
'c': i := ( i + k ) * 2;
end;
writeln( i, k )
end.